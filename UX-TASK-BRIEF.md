# MCP 安檢(mcp-guard)— UI/UX 優化任務書 v1.0

> 給 Claude Code 的執行指令。開工前先完整讀完本文件。
> 專案:https://github.com/rfobelieve-crypto/mcp-guard ｜ https://mcp-guard-iota.vercel.app

---

## 0. 角色與邊界

**你的角色**:前端 UI/UX 實作。本任務的負責人(Austin)在此專案的職責是使用者體驗與介面設計,**不是**稽核引擎本身。

**絕對不要動的部分**:
- 稽核引擎的偵測邏輯、規則、嚴重度判定
- 資料抓取與每日重新驗證的排程
- 任何後端 API 的回傳結構(除非為前端需求新增欄位,且需先提出並等待確認)

**可以動的部分**:前端呈現層、資訊架構、互動邏輯、文案呈現方式、CSS。

**共同開發者專案**:任何超出 UI/UX 範圍的改動,先停下來說明,不要自行決定。

---

## 1. 產品定位(判斷所有設計取捨的依據)

一個繁體中文的 MCP 獨立安全稽核工具。核心價值:**安裝前先知道它要什麼權限**,且每個結論都附可自行複現的證據。

現況:178 個熱門 MCP、六大檢查面向(供應鏈/權限/身分/維護/工具描述投毒/代理指令檔)、四級嚴重度、每日 05:00 自動重驗。

**最強的差異化**:意圖比對——同一個權限,依「宣稱用途」判定為合理或超標。這是既有 MCP 目錄(PulseMCP 等)沒有的語意判斷層。所有設計都不得削弱這一點的可見度。

---

## 2. 雙受眾與預設模式(重要,先讀懂再動手)

服務兩種使用者:

- **A 類 決策者**:「我現在要不要裝這個?」——需要 30 秒內得到結論
- **B 類 稽核者**:「這個 MCP 到底有什麼問題?」——需要完整證據鏈

**預設模式決定:掃描模式(A 類)為預設,完整報告(B 類)為使用者主動展開。**

理由:首頁的價值主張是「裝下去之前先知道」,這是 A 類語境;B 類的需求較少見但深度高,適合藏在一次點擊之後。B 類使用者願意多點一下,A 類使用者不願意被淹沒。

> Austin 若不同意此預設,由他在執行前直接改寫本節,不留白。

---

## Phase 0 — 現況稽核(先做這個,不要先改任何程式碼)

分析完成後產出一份 `UX-AUDIT.md`,回答以下問題。這些是外部觀察無法確認的,必須從程式碼確認:

1. `/registry` 頁面的稽核結果卡片,**預設是收合還是展開**?
2. 是否有搜尋框?篩選器(用途/結論)是即時互動還是重新整理頁面?
3. 178 筆資料是一次全部載入、分頁,還是無限捲動?首次載入的 payload 多大?
4. `/pick`(該裝哪個)頁面目前的實際互動流程是什麼?
5. CLI(`mcp-guard owner/repo`)能否查詢未收錄在 178 筆內的 repo?
6. 目前的技術棧、元件架構、樣式方案為何?
7. 行動裝置的斷點設計現況?

同時跑一次 Lighthouse(行動裝置模式),記錄 Performance / LCP / TBT 作為基準。

**Phase 0 完成後停下來,把 UX-AUDIT.md 給 Austin 看過再進 Phase 1。** 因為下面的優化項目,可能有一部分現況已經做好了。

---

## Phase 1 — P1:分層揭露(最高優先)

**問題**:單一 MCP 攤開有 15+ 項發現、上百字說明。想快速決策的人會被淹沒。

**做法**:
- 每筆稽核卡片預設只顯示:①結論徽章(已通過/需複核/不要安裝)②最高嚴重度的**一句話**摘要 ③「查看 N 項發現 →」
- 點開後才進入完整報告(現有的完整內容全部保留,不刪任何資訊)
- 展開狀態要能以 URL 保存(deep link),方便使用者分享單筆結果

**驗收標準**:未展開時,單張卡片高度不超過三行;178 筆結果可在一個螢幕高度內看到至少 6 筆。

---

## Phase 2 — P2:風險類型視覺分軌

**問題**:「npm 倉庫指向不同 repo(可能仿冒)」跟「會讀取環境變數(功能所需)」在同一個列表流裡,只靠 HIGH/LOW 區分。前者是身分詐欺疑慮,後者是例行提醒,不該用同一套視覺語言。

**做法**:展開後的發現清單,分成三軌呈現,各有獨立的圖示與色彩系統:

| 軌道 | 涵蓋 | 語意 |
|---|---|---|
| 身分與供應鏈 | 倉庫不符、typosquatting、postinstall、未鎖版本、registry 驗證層級 | 「這可能不是它宣稱的東西」 |
| 權限 | 執行指令、讀寫檔案、eval、外部主機、環境變數 | 「它要什麼、合不合理」 |
| 維護狀態 | 停更、issue 積壓、最後推送 | 「壞了有沒有人修」 |

**注意**:「超出宣稱用途」的權限發現要在權限軌道中有明確的視覺強調——這是產品最強的差異化,不能被埋沒在列表裡。

---

## Phase 3 — P3:文案的個案化

**問題**:「這個 MCP 能在你的電腦上執行系統指令」逐字重複出現在數十筆結果。一致性利於掃描,但讀起來像罐頭,與「我們逐一稽核」的敘事有落差。

**做法**:僅針對 **HIGH 級**發現,在樣板句之外加一行針對該專案的具體描述(例如點名實際的檔案與行為)。MEDIUM 以下維持樣板。

**成本警告**:這需要為每個 HIGH 發現多一次 LLM 呼叫。實作前先估算:目前有多少筆 HIGH?每日重驗是否要重跑?**先算出成本再實作,把數字告訴 Austin 決定。** 若成本不可接受,退而求其次:讓樣板句包含該專案的具體檔案名稱(這不需要 LLM)。

---

## Phase 4 — P4:空查詢引導

**問題**:生態系已有 2 萬+ MCP,收錄 178 筆。使用者查不到目標時目前可能是空白。

**做法**:查無結果時顯示 ①CLI 指令引導(`mcp-guard owner/repo`)②「提交稽核請求」入口(記錄請求的 repo 名稱)。

**附帶價值**:每筆請求都是需求訊號,可作為後續收錄優先序的依據。這是成長機制,不只是錯誤處理。

---

## 全案技術約束

- **效能底線**:每個 Phase 完成後重跑 Lighthouse,不得低於 Phase 0 基準 10 分以上
- **不得破壞**:現有的證據可追溯性(檔案路徑、原文片段)一項都不能在重構中遺失
- **繁體中文**:所有介面文案使用繁體中文,不使用簡體
- **無障礙**:嚴重度不可僅以顏色區分,需搭配文字或圖示(色盲使用者)
- **漸進增強**:任何 JS 互動失效時,內容仍應可讀

---

## 模型分工(executor–advisor)

- **Sonnet 為預設執行者**:讀碼、寫碼、重構、測試、跑 Lighthouse
- **升級呼叫 Opus 的觸發點**(全案上限 4 次,記錄實際次數):
  1. Phase 0 稽核完成後的整體架構決策
  2. 分層揭露的狀態管理方案選擇(若涉及既有架構調整)
  3. 任一 Phase 效能不達標且兩次修正無效
  4. 全案上線前審查
- **升級呼叫格式**:「任務 X、限制 A/B/C、兩條路徑與各自風險、證據 Y——選哪條、要注意什麼?」

---

## 建置記錄(對外交付物,邊做邊寫,不可事後補)

本任務對 Austin 的作品集價值來自**過程記錄**,不是介面本身。每個 Phase 完成後填寫 `BUILD-LOG.md`:

```markdown
## Phase N — [名稱]
- 起點:改動前的狀態(截圖/數據)
- 關鍵 prompt:原文
- 卡關事件:問題 → 判斷 → 解法 → 學到什麼
- 模型分工:是否升級呼叫、為了什麼、advisor 給了什麼
- 結果:改動後狀態(截圖/數據)、耗時
```

---

## 完成標準(全案 DoD)

1. `UX-AUDIT.md` 完成並經 Austin 確認
2. Phase 1–4 全數上線,通過各自驗收標準
3. Lighthouse 不低於基準 10 分以上
4. `BUILD-LOG.md` 四個 Phase 全數填寫完畢
5. 共同開發者(專案夥伴)確認未影響稽核引擎行為
