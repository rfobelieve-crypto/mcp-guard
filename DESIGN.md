# 視覺世界

> 跨 session 的設計真相源。動網站之前先讀這裡。
> 產品面的任務清單在 [TODO.md](TODO.md)。

---

## 這個網站的世界：深空觀測站

| | |
|---|---|
| **主角** | 一隻精密的稽核機器人（望遠鏡式機身、單眼鏡頭、雙機械臂），懸在深空中收集並篩選湧入的封包 |
| **材質** | 星塵、體積光、加色混合的光暈。**光是主體，線只是結構** |
| **光** | 鏡頭自體發光，冷藍白（`#A8C7FA`）；其餘全黑 |
| **情緒** | 冷靜、精密、可信。不是科幻，是天文台儀器 |
| **禁忌** | 霓虹、漸層文字、玻璃擬態、發光藍色科技感那套；機器人不准可愛化、卡通化 |

首屏動畫是**產品隱喻本身**，不是裝飾：封包從深空湧入後**排隊**，機器人
以掃描光束**逐一**檢查，依真實比例分流——通過的收進綠色的流、需複核的
滯留在待判環、拒絕的彈回深空。「排隊、逐一」是重點：總表那一百多份報告
正是這樣被產出的。劇本必須反映真實資料，所以只能手寫——生成的影片是
固定畫面，演不出「逐一評估」。

機器人素材是 Higgsfield 生成、`make_robot.py` 後製（去掉自帶光球與
光束——那兩樣歸 canvas 動態演）。素材以 **screen 混合**繪製：黑即透明，
與整個首屏「加色混合」同一套物理，也免去去背白邊。改素材重跑
`python make_robot.py`。

---

## 兩個區域，兩套法則

**首屏可以電影感，總表必須樸素。** 這不是妥協，是兩者的任務不同。

- **首屏**（說服）：讓人理解「MCP 會拿到什麼」。可以慢、可以大、可以美。
- **總表**（掃讀）：18 個專案的結論要能在幾秒內讀完。動態會傷害掃讀效率，
  而對信任型產品，過度炫技反而扣信譽分——Have I Been Pwned、Snyk、
  Socket.dev 都刻意樸素。

已經因此拒絕過的東西：總表的列動畫、3px 彩色左邊框（狀態已由標籤表達，
彩條純屬冗餘，而且那是一眼認得出的模板感來源）。

---

## 技術立場

- **零相依**。首屏動畫是手寫 canvas 狀態機，不是 WebGL；後端
  （`api/rpc.py`）只用 Python 標準函式庫。
- **產生器**。`site.py` 產出全部頁面；script 一律外部檔——CSP 的
  `script-src 'self'` 因此成立，行內指令碼一律拒收。對一個安全稽核
  網站，自己的內容安全政策就是招牌的一部分。
- **深色世界**。淺色主題只在使用者明確指定時尊重，不做「跟隨系統」。
- **無資料庫**。session 是簽名 cookie、收藏在 localStorage、掃描請求
  開成公開 issue。我們拿不到的資料，就不必解釋怎麼保管。
  登入與提交的設定見 [docs/DEPLOY.md](docs/DEPLOY.md)。

### 分享圖（OG）

四頁各一張，**用網站自己的場景渲染**，不是生成的——分享卡片和點進去看到的
畫面必須是同一個世界，而且圖上帶得了真實數字。改動標題或數字後重製：

```bash
python site.py && python make_og.py      # 產生 assets/og/*.html（1200×630）
# 用瀏覽器逐一開啟截圖，裁切 (0,0,1200,630)，存成同名 .jpg
python site.py                           # 複製到 site/og/
```

存 JPEG 不存 PNG：來源是截圖（本就有失真），PNG 只會讓體積大 5 倍。

### 字體

標題用 `DisplayTC`（思源黑體子集，38KB，可變字重 100–900），內文用系統字。

「繁中字型 5–10MB 所以中文網站做不出 premium 排版」是個假死結——**子集化**
就解了：全站標題只用到 91 個中日韓字。改動任何標題文字後要重跑：

```bash
python site.py && python make_font.py    # 產物 assets/fonts/display.woff2 進版控
```

缺字會靜默回落系統字，不會壞掉，但會不一致。

---

## 怎麼對 Claude 描述視覺需求

「做得好看一點」「參考某某網站」得到的一定是安全平庸的東西——因為缺的不是
形容詞，是**決定**。有效的需求就這五件事：

1. **主角是什麼實體** — 不是「一些動效」，是「一顆發光的核心」「一道鉻金屬閘門」
2. **材質與光** — 鉻／玻璃／霧／星塵／顆粒？光是冷是暖、從哪來？
3. **情緒兩個字** — 冷峻、克制、狂躁、溫柔
4. **禁忌** — 「什麼絕對不要」比「要什麼」更能拉開品質
5. **不准動的東西** — 哪些內容、功能、既有決策是產品真實

範例（這就是本次首屏改版真正被執行的那段需求）：

> 首屏做成**深空觀測站**：封包像星塵從遠處湧入，中央是一顆**有體積**的稽核
> 核心，冷藍白光，其餘全黑。要**冷靜精密**，不要科技感藍色發光那套俗氣。
> **總表不准動**——那是信任型產品，樸素是刻意的。零相依，純 canvas 手寫。
