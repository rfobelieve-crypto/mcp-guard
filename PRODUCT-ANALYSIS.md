# mcp-guard 產品分析：從稽核工具到可付費服務

> 基於實際程式碼與 `reports/data.json` 的真實資料（2026-07-28 掃描結果）撰寫，
> 非依據 README 複述。所有數字皆可自行複現。

---

## 第一部分：這個系統現在實際有什麼

### 1.1 稽核引擎（`mcp_guard/`，1,532 行 Python，零第三方相依）

| 模組 | 行數 | 職責 |
|---|---|---|
| `fetch.py` | 266 | 唯讀取得：GitHub（透過 `gh` CLI）、npm registry、PyPI、官方 MCP registry |
| `checks.py` | 855 | 六項檢查的全部偵測邏輯 |
| `profile.py` | 91 | 從專案自述推斷「能力分類」（瀏覽器／程式碼／資料庫／桌面控制…） |
| `domain.py` | 88 | 推斷「應用領域」（金融／健康／網站／資料分析…） |
| `report.py` | 104 | 判定結論 + 繁中 Markdown／終端機報告 |
| `cli.py` | 58 | CLI 入口，含 CI 可用的退出碼 |
| `agentfiles.py` | 68 | 辨識會被 AI 客戶端自動載入的指令檔路徑 |

**安全設計上值得注意的兩點**（這是可以拿出來說的專業度，不是行銷話術）：

1. **原始碼壓縮檔在記憶體中解析，不落地解壓**（`fetch.py` 開頭註解）——因此
   結構上不存在 zip-slip／path traversal 風險。一個稽核工具自己被稽核對象
   打穿，是最難看的失敗。
2. **manifest 檔案不受檔數上限限制**。這是從一個真實 bug 學到的：
   `modelcontextprotocol/inspector` 有 400+ 個檔案，`package.json` 曾被一般
   原始碼擠出 400 檔上限，導致同一個專案前後兩天結論不同。對「每日重掃」
   的產品來說，結論不穩定比漏報更致命。

### 1.2 六項檢查各自解決什麼問題

| 檢查 | 抓什麼 | 對使用者的意義 |
|---|---|---|
| **身分** | repo／作者是否存在、新建搶註、已封存、授權 | 「社群瘋傳的安裝指令，其實 repo 根本不存在」——名稱可被任意搶註 |
| **供應鏈** | npm `pre/postinstall`、Python `setup.py` 安裝期執行、依賴未鎖版、npm↔repo 對不上 | 「你還沒開始用，程式碼就已經跑過一次」 |
| **權限** | 子行程、`eval`、檔案讀寫、環境變數、外連主機、需要的金鑰 | **依宣稱用途判定合理或越權**（見下方 1.3） |
| **工具描述投毒** | 描述中要求模型忽略指令／隱瞞使用者／強制先呼叫某工具、零寬與雙向覆寫字元 | MCP 獨有攻擊面：一段模型讀得到、你讀不到的指令 |
| **代理指令檔** | `SKILL.md`／`AGENTS.md`／`CLAUDE.md`／`.cursorrules` | 這些會被 AI 客戶端**自動讀進上下文**，比 tool description 更直接 |
| **維護** | 最後更新距今、未處理 issue 數 | 「壞了有沒有人修」 |

### 1.3 真正的護城河：意圖比對（`check_permissions`）

程式碼註解記錄了這個設計的由來（`checks.py:348-354`）：

> 初版所有「會開子行程」一律判 HIGH，導致 18 個專案有 15 個是同一個 🟡，
> 完全沒有區辨度——但桌面控制類 MCP 開 shell 本來就是它的功能。

現在的邏輯是：先推斷這個工具**自稱**是什麼（`infer_profile`），再逐項判斷
每個能力是本份（LOW，標示「符合宣稱用途」）還是越權（HIGH，標示
「⚠ 超出宣稱用途」）。

**實際資料驗證這個機制有效：**

```
178 個專案中，51 個（29%）被抓到「超出宣稱用途」，共 82 項發現
```

這是市面上所有 MCP 目錄（PulseMCP、Smithery、mcp.so、Glama）都**沒有**的
語意判斷層。他們回答「有哪些 MCP」，這個工具回答「它要的東西合不合理」。

### 1.4 目前的資料規模與分布（真實數字）

```
專案數：178          掃描耗時：652 秒（≈3.7 秒/專案）
結論：🟢 110（62%）  🟡 68（38%）  🔴 0
發現總數：2,044      INFO 1,207 / LOW 515 / MEDIUM 212 / HIGH 110
```

各檢查面向命中數：權限 890、身分 373、維護 227、代理指令檔 194、
供應鏈 180、工具描述投毒 180。

應用領域分布：通用 106、金融與交易 17、資料分析 11、基礎設施 10、
網站前端 9、影音設計 8、文書生產力 8、健康醫療 7、研究學術 2。

發布者驗證層級：GitHub 帳號 128、網域驗證 35、未登錄 registry 15。

星數：最高 88,977、中位數 1,422、超過 10k 有 28 個、低於 1k 有 80 個
（刻意納入低星專案——權限天生就大的類型，正是最需要在安裝前看懂的）。

### 1.5 網站四頁各自的功能

| 頁面 | 功能 | 目前狀態 |
|---|---|---|
| `/` | 首屏 + 三個入口 | 純導覽，**沒有任何輸入框** |
| `/registry` | 178 筆完整結果，即時搜尋 + 三維篩選（結論／能力／領域） | 全部烤進 1.04MB 單一 HTML |
| `/pick` | 「你想讓 AI 幫你做什麼」→ 依領域（8 類）與能力（9 類）分組，各列前 5 | 每列直連 GitHub，**不連回稽核詳情** |
| `/method` | 六項檢查方法論 | 靜態說明 |
| `/trust` | 誤報紀錄、更正政策、能力邊界 | 信任基礎，寫得很好 |

### 1.6 每日重掃機制（`.github/workflows/daily-scan.yml`）

台北時間每天 05:00 自動重掃，**掃描前先跑紅隊測試，未通過就中止**——
「一個連自己紅隊測試都沒過的掃描器，不該對外發布任何結論」。結果寫回 repo，
形成可稽核的歷史。

這個機制是目前**最被低估的資產**：它產生的是「時間序列」，而不只是快照。
但網站上只呈現了最新一次的結果，歷史差異完全沒有被使用。

---

## 第二部分：現在為什麼變不了現

### 2.1 覆蓋率的殘酷數字

官方 registry 有約 **13,800 個**不重複的 GitHub 專案（`sync_targets.py` 註解），
目前收錄 178 個 = **1.3%**。

這代表：**使用者有 98.7% 的機率查不到他想查的那一個。**

而查不到時，目前的網站給他的是一行「沒有符合條件的專案」。

### 2.2 這是整個產品最貴的一個漏洞

想清楚這個場景：

> 一個開發者在 Twitter 看到有人推薦某個 MCP，複製了安裝指令，
> 心裡有點不放心，Google 搜到這個網站，輸入那個名字——**查無結果**。

這是**意圖最強的一刻**：他正要安裝，他主動在找理由說服自己或阻止自己。
這一刻的價值遠高於任何瀏覽行為。

而技術上，這一刻是**完全可以服務的**：CLI 早就能查任意 repo
（`cli.py:34` 呼叫 `collect(args.target)` 即時抓取分析，與那 178 筆完全無關），
單次只要 3.7 秒。

**網站與 CLI 之間缺的那座橋，就是整個商業模式的入口。**

### 2.3 其他三個結構性缺口

**沒有身分。** 純靜態站 = 完全匿名。不知道誰來過、誰在乎什麼、誰該被通知。

**沒有回訪理由。** 查完就走。但風險是會變的——今天乾淨的專案，明天可能
被轉手、加了 postinstall、改了 tool description。每日重掃已經在跑了，
但沒有任何人被告知。

**沒有工作流整合。** CLI 有 CI 退出碼，但沒有託管版本。真正在乎這件事的
團隊，需要它出現在 pipeline 裡，不是出現在一個網頁上。

---

## 第三部分：付費設計

### 3.1 先講清楚什麼**不能**收費

引擎是 MIT 開源的，任何人 `pip install .` 就能掃。所以：

- **178 筆稽核總表：永久免費。** 這是可信度引擎與自然流量來源，收費等於自殺。
- **方法論與誤報紀錄：永久免費。** 這是信任的證明，不是產品功能。
- **CLI 本身：永久免費開源。** 「一個稽核工具不該反過來擴大你的供應鏈」
  這個立場如果只是行銷話術，整個產品的可信度會崩掉。

### 3.2 那使用者到底付錢買什麼

答案不是「掃描」，是這四件事：

1. **連續性** — 跑一次 CLI 是免費的；**知道它什麼時候變了**需要基礎設施。
2. **彙總** — 「我裝的這 12 個加起來長什麼樣」、「我團隊裝了什麼」。
3. **不用自己跑** — 不用裝 Python、不用 `gh auth login`、不用維護。
4. **政策執行** — 把判斷變成關卡（gate），而不只是報告。

這是典型且誠實的 open-core 模型：工具免費且可稽核（安全產品的信任前提），
服務收費。

### 3.3 分層設計

#### Tier 0：免費、無需註冊
- 178 筆稽核總表、六項檢查方法論、誤報紀錄與更正政策
- **每日 3 次隨選掃描**（不需註冊，用 IP 限流）
  → 目的：讓「查無結果」這個死路，變成產品最好的展示

#### Tier 1：免費、需 GitHub 登入
- 隨選掃描提高到每日 20 次
- **監控清單上限 3 個**（讓他嚐到警報的價值）
- 掃描歷史保留 7 天

→ 目的：換取身分。GitHub OAuth 對這個受眾是零摩擦的，
而且同一組授權之後可以解鎖私有 repo 掃描。

#### Tier 2：個人版（建議 US$7/月 或 US$70/年）
- **無限隨選掃描**
- **無限監控清單 + 變更警報**（這是核心價值，見 3.4）
- 私有 repo 掃描（用他自己的 OAuth 授權）
- 完整 Markdown／PDF 報告匯出
- 掃描歷史無限保留 + 版本差異對照

#### Tier 3：團隊版（建議 US$39/月起，含 5 席）
- **組織政策**：例如「禁止安裝有『超出宣稱用途』shell 權限的 MCP」、
  「只允許網域驗證的發布者」
- **CI／CD 整合**：託管的 GitHub Action + API token，PR 有人加了新 MCP 就擋
- **團隊資產盤點**：我們團隊實際在跑哪些 MCP、誰裝的、什麼時候
- 合規稽核輸出（CSV／JSON／PDF）
- SSO

#### Tier 4：企業版（客製報價）
- 自架引擎（含私有 registry 掃描）
- 新攻擊樣式更新的 SLA
- 客製檢查規則

### 3.4 最強的付費理由：變更警報

這是**只有這個產品做得到**的功能，因為只有它每天重掃 + 保留歷史。

值得警報的事件（都是現有引擎已經能偵測的）：

| 事件 | 為什麼可怕 |
|---|---|
| 新增 `postinstall` 腳本 | 你更新套件的那一刻，新程式碼就跑了 |
| 新增「超出宣稱用途」的權限 | 一個 API 串接工具突然會開 shell |
| tool description 內容改變 | 投毒最隱蔽的下手點——你不會重讀它 |
| 代理指令檔（SKILL.md 等）改變 | 會被自動讀進模型上下文 |
| repo 轉手／改名／封存 | 供應鏈接管攻擊的典型前兆 |
| npm 套件指向的 repo 變了 | 仿冒或劫持 |

一句話的行銷語言：**「你裝的時候是安全的。之後呢？」**

### 3.5 經濟模型（實算，不是估）

- 單次掃描：3.7 秒 CPU + 約 3-4 次 GitHub API 呼叫 + 一次 tarball 下載
- GitHub API 限制：認證後 5,000 次/小時 → 單一 token 約可支撐 **1,200 次掃描/小時**
- 每日監控重掃：1,000 位付費用戶 × 各 10 個監控目標 = 10,000 次
  → 但熱門專案高度重疊，去重後每日約 2,000 次獨立掃描 → **不到 2 小時的預算**

**結論：邊際成本趨近於零，毛利極高。真正的成本是工程與基礎設施，不是每次掃描。**

---

## 第四部分：讓使用者「輕鬆上手」的具體改動

### 4.1 最重要的一個改動：首頁必須是一個輸入框

目前的漏斗：
```
首頁（讀文案）→ 稽核總表 → 捲 178 筆 → 沒找到 → 離開
```

應該是：
```
首頁：一個大輸入框「貼上你要裝的 MCP」
  → 已收錄：秒開快取結果
  → 未收錄：即時掃描（3.7 秒，顯示進度）
  → 結果頁底部：「加入監控清單」→ 登入 → 回訪理由建立
```

價值主張是「安裝前先知道」，那麼**畫面上第一個東西就該是「告訴我你要裝什麼」**。
現在的首頁在解釋產品，但這個產品不需要解釋——它需要被使用。

### 4.2 輸入格式要寬容到近乎縱容

使用者手上會有的東西五花八門，全部都該能貼：

```
owner/repo
https://github.com/owner/repo
npm:some-mcp-server
npx -y @scope/some-mcp-server          ← 從安裝指令直接貼
{"command":"npx","args":["-y","..."]}  ← 從 claude_desktop_config.json 直接貼
```

最後兩種是關鍵：**使用者手上真正有的是「安裝指令」或「設定檔片段」**，
不是乾淨的 `owner/repo`。要求他們自己轉換，就是在漏斗最上緣設關卡。

### 4.3 進階做法：整份設定檔健檢

讓使用者直接上傳／貼上整個 `claude_desktop_config.json`（或 Cursor、
Cline 的設定檔），一次盤點他**已經裝了**的所有 MCP。

這一步的說服力遠大於單筆查詢：
> 「你現在正在跑 9 個 MCP。其中 3 個要求了超出它們宣稱用途的權限。」

而且這個動作天然產出一份監控清單——**免費用戶做完健檢，就已經有了
訂閱的理由**。轉換路徑幾乎是自動的。

### 4.4 結果頁必須可分享（deep link）

目前 `/registry` 個別展開狀態不寫入網址（`UX-AUDIT.md` 已確認）。
每一筆結果都該有獨立網址 `/scan/owner/repo`，因為：

- 「我可不可以裝這個」在團隊裡是**對話**，不是獨自的決定
- 每一次分享都是一次免費曝光，而且是帶著具體證據的曝光
- 這是這類產品最主要的自然成長來源

### 4.5 免費／付費的邊界要在正確的地方

**絕對不要**把「結論」藏在付費牆後面。那會摧毀信任——
使用者會合理懷疑「你是不是為了收錢才說它有問題」。

正確的邊界：
- 結論、發現、證據 → **永遠免費可見**
- 持續監控、歷史差異、團隊功能、政策執行 → 付費

一句話：**免費看現在，付費看變化。**

---

## 第五部分：需要面對的三個難題

### 5.1 這需要真正的後端（現有架構的終點）

目前是零相依、零建置的純靜態站（`vercel.json` 寫死 `framework: null`）。
要做到上述任何一項付費功能，都需要：

1. **API 服務** — 隨選掃描（tarball 下載與解析吃記憶體，edge runtime 不夠，
   需要常駐 worker 或 serverless with 較高記憶體上限）
2. **資料庫** — 使用者、監控清單、掃描歷史（差異對照的前提）
3. **認證** — GitHub OAuth（對這個受眾零摩擦，且解鎖私有 repo 掃描）
4. **金流** — Stripe
5. **排程佇列** — 每日監控重掃

**這是誠實的代價：從「靜態站」變成「產品」，就是零後端的優雅結束的那一刻。**

### 5.2 商業化會被質疑動機（必須主動處理）

一個安全工具開始收費，一定會有人問：
> 「你現在是不是有動機多找一點問題？」

這個問題不能等別人問，要自己先在 `/trust` 頁面回答。可信的做法：

- **引擎維持開源可稽核** — 任何人都能自己跑一遍驗證結論
- **誤報紀錄繼續公開** — 現在寫得很好，這是最有力的證據
- **結論永不因付費而改變** — 明文承諾並寫進定價頁
- **付費買的是「監控與整合」，不是「更寬鬆或更嚴格的判定」** — 定價邏輯本身要公開

現有的 `/trust` 頁面已經做對了最難的部分（主動承認誤報、寫出更正政策），
這個資產在商業化之後只會更值錢。

### 5.3 與目前擱置中的「電影感重做」直接衝突

`REDESIGN-PROPOSAL.md` 那個決定目前還在等確認。但本文件描述的方向意味著：

**首頁的核心不是視覺，是那個輸入框。**

一個以 3D 場景為主角的 hero，跟一個以「立刻查」為主角的 hero，是兩個不同的產品。
而且實測已經證明（見 PR #1 的 commit `ab70af8`）：Next.js/React 的 hydration
成本讓 LCP 卡在 5.4 秒，對一個「使用者急著查一件事」的工具型網站，這個代價
比對一個品牌型網站嚴重得多。

**建議：先確定產品方向（工具型 vs 品牌型），再決定視覺方向。**
順序反過來的話，很可能做完一個很美但轉換不了的首頁。

---

## 第六部分：優先順序建議

| 優先 | 項目 | 為什麼是這個順序 |
|---|---|---|
| **P0** | 首頁改成查詢框 + 隨選掃描 API | 覆蓋率 1.3% 的死路，是目前最貴的漏洞；意圖最強的一刻現在完全沒被服務 |
| **P0** | 寬容的輸入解析（含安裝指令／設定檔片段） | 漏斗最上緣的摩擦，成本低、影響大 |
| **P1** | GitHub OAuth + 監控清單（免費 3 個） | 取得身分 + 建立回訪理由，付費轉換的前置條件 |
| **P1** | 每個結果獨立網址（deep link） | 自然成長的主要來源，且技術成本低 |
| **P2** | 變更警報（email／webhook） | 核心付費理由，但需要 P1 的資料結構先到位 |
| **P2** | 整份設定檔健檢 | 最強的轉換工具，但需要 P0 的掃描 API |
| **P3** | 團隊功能、CI 整合、政策引擎 | 更高客單價，但要等個人版驗證需求真實存在 |

**關於既有的 `UX-TASK-BRIEF.md` Phase 1-4：**
分層揭露（Phase 1）與空查詢引導（Phase 4）與本文件方向完全一致，
應該保留；風險分軌（Phase 2）在結果頁仍然需要。文案個案化（Phase 3）
建議降低優先度——它改善的是質感，不是轉換。

---

## 附錄：一頁總結

**這個產品現在是什麼**：一個技術紮實、判斷獨到（29% 的專案被抓到權限越權）、
信任基礎良好（公開誤報紀錄）的靜態稽核報告站。

**它現在不是什麼**：一個使用者能拿自己手上那個 MCP 來查的工具。

**最短的變現路徑**：把首頁變成輸入框 → 服務那 98.7% 查不到的長尾 →
用 GitHub 登入換身分 → 用「你裝的東西變了」這個只有你做得到的警報收費。

**最該避免的錯誤**：把結論藏在付費牆後面。免費看現在，付費看變化。
