# 網站全面重新設計提案 — 電影感/高端創意站 vs. 現有架構

> 用途：給共同開發者評估用的決策文件，**非實作規格**。若雙方確認要往這個方向走，
> 文末附階段化遷移建議，降低一次性重寫的失敗成本。
>
> 觸發：Austin 提出將整站改造成「電影感、高端、沉浸式創意代理商風格」，含
> 首頁 photorealistic 3D 機器人場景，技術棧改為 Next.js + Tailwind +
> Framer Motion + React Three Fiber。此提案**取代** `UX-TASK-BRIEF.md` 的
> Phase 1-4 漸進式優化計畫。

---

## 1. 現況(Phase 0 已驗證的事實)

| 項目 | 現況 |
|---|---|
| 技術棧 | 純 Python(`site.py`，1,323 行)產生靜態 HTML，零 JS 框架、零建置步驟 |
| 部署 | Vercel `framework: null`，直接發布 `site/` 資料夾，無 build command |
| 依賴 | 零第三方前端依賴；CLI 端 README 明文「零第三方相依——一個稽核工具不該反過來擴大你的供應鏈」 |
| 效能基準(行動裝置 Lighthouse) | 首頁 97 分(LCP 1.4s)／`/pick` 100 分(LCP 1.4s)／`/registry` 54 分(LCP 6.3s，178 筆全載入所致) |
| 視覺立場(`site.py` docstring 原文) | 「總表刻意不套電影感動態：那會傷害掃讀效率，而且對信任型產品來說過度炫技反而扣分」 |
| 全案技術約束(`UX-TASK-BRIEF.md`) | 「每個 Phase 完成後重跑 Lighthouse，不得低於 Phase 0 基準 10 分以上」 |

## 2. 新需求摘要

- 完全移除「AI 生成工具網站」的通用感，做到「高端創意工作室手筆」的質感
- 首頁 hero 為 photorealistic 3D 機器人，執行資料蒐集與風險過濾的視覺化
- 配色：近黑背景(#050507–#0A0A0C) + 克制的青色(#00E5FF)/紫色點綴
- 大型 display 字體標題 + 精緻內文字體(非系統字/Inter)
- 動態：滑鼠跟隨光源、粒子飄浮、卡片懸浮傾斜、慢速有份量的攝影機視差
- 技術棧：Next.js + Tailwind + Framer Motion + React Three Fiber(3D 機器人場景)
- 內容結構(稽核總覽數字、稽核總表、方法論、可信度)維持不變，只換視覺呈現層

## 3. 直接衝突點(需要共同開發者明確表態，不是我方單邊判斷)

### 3.1 產品定位衝突：信任 vs. 炫技
現有站台的敘事是「唯讀、不執行、可自行複現的證據」——克制本身就是可信度的一部分。
高端創意代理商風格(粒子、全像卡片、電影感攝影機)通常用在品牌/行銷型網站，
用在「幫你判斷這個工具能不能裝」的安全稽核平台上，有實質風險讓使用者
產生「這是不是在用視覺效果掩蓋什麼」的觀感——這跟產品最強差異化
(「意圖比對：同一權限依宣稱用途判定合理或超標」)的可信基礎是同一件事的
兩面，值得共同開發者從產品定位角度明確拍板，不只是視覺喜好問題。

### 3.2 效能衝突：與既有 Lighthouse 底線直接牴觸
- 現況首頁/`/pick` 是 97-100 分的建置，這是 R3F photorealistic 3D 場景
  幾乎不可能維持的門檻(RSF + 高品質 3D 資產通常會讓 TBT/LCP 明顯上升，
  即使做了 code-splitting 和 lazy load)。
- `/registry` 現在 54 分本身已經是待修的問題(Phase 1 目標)；若改用
  Next.js 全面重寫，178 筆資料的載入策略要重新設計(SSG/ISR/分頁)，
  這件事在新架構下仍然要解決，並不會因為換框架而自動變好。
- 建議：若採用新方向，效能底線需要重新協商(例如首頁 hero 場景可以有
  獨立的效能預算，但 `/registry`、`/method`、`/trust` 這類需要掃讀的頁面
  應維持接近現有的克制與速度)。

### 3.3 依賴與供應鏈：跟產品自身立場的反差
README 對 CLI 明講「零第三方相依——一個稽核工具不該反過來擴大你的供應鏈」。
網站雖是不同的程式碼路徑，但同一個品牌下，「教別人檢查 npm supply chain
風險的工具，自己的官網卻疊了 Next.js + Tailwind + Framer Motion + R3F +
其各自的遞移依賴」——這在敘事上是個反差，值得共同開發者評估是否要在
`/trust` 頁面主動說明立場一致性，或者接受這只是網站與 CLI 各自獨立的
判斷標準。

### 3.4 架構：從零建置靜態站到需要 Node build pipeline
- 現況 Vercel 設定是「framework: null，無 build command」，換成 Next.js
  代表整個部署管線要重建(build step、Node runtime、可能的 ISR/edge
  function 設定)。
- 現有 GitHub Pages 備援部署(`site.py` docstring 提到同一份 HTML 也部署在
  GitHub Pages 上)在純靜態輸出下是免費的；Next.js 若用到 server
  component/ISR，GitHub Pages 備援機制需要重新設計或放棄。

## 4. 如果共同開發者同意，建議的降風險做法(不建議一次性全面重寫)

1. **先做視覺原型，不動 production 部署**：在獨立分支/獨立 Vercel
   preview 上，先只做首頁(含 3D 機器人 hero)，驗證：實際 Lighthouse
   分數、真實裝置上的觀感、是否真的達成「高端而非炫技」的效果。
2. **`/registry`、`/method`、`/trust` 晚一點換皮**：這三頁的核心價值是
   「快速掃讀、找到證據」，優先度應該是先確認新視覺系統套用在**大量
   文字/表格內容**上不會犧牲掃讀效率，而不是直接套用首頁的電影感語言。
3. **3D 機器人場景務必做效能分級**：低階裝置/`prefers-reduced-motion`
   使用者應該有純靜態/低動態的替代版本(現有 `REVEAL_JS` 已經有
   `prefers-reduced-motion` 判斷，新架構應該延續這個原則，不能倒退)。
4. **廢棄計畫要明確**：現有 `site.py` 產生器、`REVEAL_JS`/`FILTER_JS`/
   `SCENE_JS` 三段手寫 vanilla JS、以及 GitHub Pages 備援機制，若全面
   改用 Next.js，這些會被完全取代——需要共同開發者確認這些現有投入
   可以捨棄。
5. **`UX-TASK-BRIEF.md` 需要正式定案**：目前 Phase 1-4 是否作廢、或是
   併入新設計(例如分層揭露、風險分軌、空查詢引導這些**互動邏輯**其實
   跟視覺風格無關，可以在新皮膚下延續)，需要一份明確結論，避免兩份
   計畫同時進行造成混亂。

## 5. 待共同開發者決定的問題清單

- [ ] 是否接受效能底線從「不低於 Phase 0 基準 10 分」放寬，改成分頁面
      各自訂定新的效能預算？
- [ ] 首頁以外的頁面(尤其 `/registry`)是否也要套用電影感語言，還是維持
      克制的掃讀優先設計？
- [ ] 是否接受從零依賴靜態站遷移到 Next.js + 完整前端依賴鏈，以及隨之而
      來的建置管線、GitHub Pages 備援重新設計？
- [ ] `UX-TASK-BRIEF.md` 的 Phase 1-4 是正式作廢，還是其中的互動邏輯
      (分層揭露、風險分軌、空查詢引導)要移植到新設計裡？
- [ ] 是否需要在 `/trust` 頁面主動說明「網站前端」與「CLI 稽核引擎」在
      依賴哲學上的差異，維持敘事一致性？

---

*本文件由 Phase 0 現況稽核延伸產生，僅整理決策所需的事實與衝突點，
不包含任何程式碼變更。*
